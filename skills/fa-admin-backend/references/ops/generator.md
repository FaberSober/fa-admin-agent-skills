# 代码生成器

## 启动

1. 启动 `fa-generator` 模块。
2. 访问 `http://localhost:7777`。
3. 选择表生成单表脚手架。

## 使用

- 后端：复制生成的 `main` 目录增量合入，人工修剪包名、基类继承和多余代码。
- 前端：复制生成的页面目录到对应 feature，更新 `types/index.ts` 和 `configs`。
- 生成后核对：包名、表名、主键类型、API 路径、基类继承、是否重复生成基类已有 CRUD。

生成代码不是最终规范，必须与相邻手写模块对齐。
